def DelTrailingSlash(path):
    path=string.strip(path)
    if path == '/':  
	return path            # for root
    if (path[-1] == '/'):
	path = path[:-1]       # get rid of trailing slashes
    return path
